function Layout(props) {
    //properties ---------------------
    //hooks ---------------------
    //context ---------------------
    //methods ---------------------
    //view ---------------------
    return (
        <div>
            {props.children}
        </div>
    )
}

export default Layout;